import React from 'react';
import Menu from './Menu';
import Slider from './Slider';

function Home(props) {
    return (
        <div>
            <Slider/>
            <Menu/>
        </div>
    );
}

export default Home;